<!-- Page Header -->
<div class="mb-8">
  <h1 class="text-2xl font-bold text-gray-900"><%= t('my.tutorial_settings.show.title') %></h1>
  <p class="mt-1 text-sm text-gray-500"><%= t('my.tutorial_settings.show.subtitle') %></p>
</div>

<!-- Settings Form -->
<div class="bg-white rounded-xl shadow-sm border border-gray-100 overflow-hidden mb-8">
  <div class="px-6 py-4 border-b border-gray-100">
    <h2 class="text-lg font-semibold text-gray-900"><%= t('my.tutorial_settings.show.display_settings') %></h2>
  </div>
  <div class="p-6">
    <%= form_with url: my_tutorial_settings_path, method: :patch, local: true, class: "space-y-6" do |f| %>
      <div class="space-y-4">
        <!-- Show Tutorials -->
        <div class="flex items-start">
          <div class="flex items-center h-5">
            <%= f.check_box "tutorial_settings[show_tutorials]",
                { checked: current_user.tutorial_enabled?, class: "h-4 w-4 text-indigo-600 border-gray-300 rounded focus:ring-indigo-500" },
                "true", "false" %>
          </div>
          <div class="ml-3 text-sm">
            <%= f.label "tutorial_settings[show_tutorials]", t('my.tutorial_settings.show.show_tutorials_label'), class: "font-medium text-gray-700" %>
            <p class="text-gray-500"><%= t('my.tutorial_settings.show.show_tutorials_hint') %></p>
          </div>
        </div>

        <!-- Context Help -->
        <div class="flex items-start">
          <div class="flex items-center h-5">
            <%= f.check_box "tutorial_settings[show_context_help]",
                { checked: current_user.context_help_enabled?, class: "h-4 w-4 text-indigo-600 border-gray-300 rounded focus:ring-indigo-500" },
                "true", "false" %>
          </div>
          <div class="ml-3 text-sm">
            <%= f.label "tutorial_settings[show_context_help]", t('my.tutorial_settings.show.context_help_label'), class: "font-medium text-gray-700" %>
            <p class="text-gray-500"><%= t('my.tutorial_settings.show.context_help_hint') %></p>
          </div>
        </div>

        <!-- Reduced Motion -->
        <div class="flex items-start">
          <div class="flex items-center h-5">
            <%= f.check_box "tutorial_settings[reduced_motion]",
                { checked: current_user.reduced_motion?, class: "h-4 w-4 text-indigo-600 border-gray-300 rounded focus:ring-indigo-500" },
                "true", "false" %>
          </div>
          <div class="ml-3 text-sm">
            <%= f.label "tutorial_settings[reduced_motion]", t('my.tutorial_settings.show.reduced_motion_label'), class: "font-medium text-gray-700" %>
            <p class="text-gray-500"><%= t('my.tutorial_settings.show.reduced_motion_hint') %></p>
          </div>
        </div>
      </div>

      <div class="pt-4 border-t border-gray-100">
        <%= f.submit t('my.tutorial_settings.show.save_settings'), class: "px-4 py-2 bg-indigo-600 hover:bg-indigo-700 text-white font-medium rounded-lg cursor-pointer transition-colors" %>
      </div>
    <% end %>
  </div>
</div>

<!-- Tutorial Progress -->
<div class="bg-white rounded-xl shadow-sm border border-gray-100 overflow-hidden">
  <div class="px-6 py-4 border-b border-gray-100">
    <h2 class="text-lg font-semibold text-gray-900"><%= t('my.tutorial_settings.show.progress_title') %></h2>
    <p class="mt-1 text-sm text-gray-500"><%= t('my.tutorial_settings.show.progress_subtitle') %></p>
  </div>
  <div class="divide-y divide-gray-100">
    <% @tutorial_progresses.each do |item| %>
      <div class="p-6 flex items-center justify-between">
        <div class="flex-1">
          <h3 class="font-medium text-gray-900"><%= item[:label] %></h3>
          <p class="mt-1 text-sm text-gray-500"><%= item[:description] %></p>
          <div class="mt-2 flex items-center space-x-4">
            <!-- Progress Bar -->
            <div class="flex-1 max-w-xs">
              <div class="h-2 bg-gray-200 rounded-full overflow-hidden">
                <% progress_percentage = item[:progress].persisted? ? item[:progress].progress_percentage : 0 %>
                <div class="h-full bg-indigo-600 rounded-full transition-all duration-300"
                     style="width: <%= progress_percentage %>%"></div>
              </div>
            </div>
            <!-- Status -->
            <span class="text-sm <%= tutorial_status_class(item[:type]) %> px-2 py-0.5 rounded-full">
              <% if item[:progress].completed? %>
                <%= t('my.tutorial_settings.show.status_completed') %>
              <% elsif item[:progress].skipped? %>
                <%= t('my.tutorial_settings.show.status_skipped') %>
              <% elsif item[:progress].persisted? && item[:progress].current_step_position > 1 %>
                <%= t('my.tutorial_settings.show.status_in_progress', current: item[:progress].current_step_position - 1, total: item[:step_count]) %>
              <% else %>
                <%= t('my.tutorial_settings.show.status_not_started') %>
              <% end %>
            </span>
          </div>
        </div>

        <!-- Actions -->
        <div class="ml-6 flex items-center space-x-2">
          <% if item[:progress].completed? || item[:progress].skipped? || (item[:progress].persisted? && item[:progress].current_step_position > 1) %>
            <%= button_to t('my.tutorial_settings.show.reset'),
                reset_tutorial_path(tutorial_type: item[:type]),
                method: :post,
                class: "px-3 py-1.5 text-sm border border-gray-300 rounded-lg text-gray-700 hover:bg-gray-50 transition-colors",
                data: { turbo_confirm: t('my.tutorial_settings.show.reset_confirm') } %>
          <% end %>
        </div>
      </div>
    <% end %>
  </div>
</div>

<!-- Back Link -->
<div class="mt-6">
  <%= link_to my_profile_path, class: "inline-flex items-center text-sm text-gray-500 hover:text-gray-700" do %>
    <svg class="mr-1 h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18" />
    </svg>
    <%= t('my.tutorial_settings.show.back_to_profile') %>
  <% end %>
</div>
