<% content_for :title, t('admin.categories.new.title') %>

<div class="mb-6">
  <%= link_to t('admin.categories.new.back'), admin_categories_path, class: "text-sm text-gray-500 hover:text-gray-700" %>
</div>

<div class="bg-white shadow sm:rounded-lg">
  <div class="px-4 py-5 sm:p-6">
    <h3 class="text-lg leading-6 font-medium text-gray-900 mb-6"><%= t('admin.categories.new.heading') %></h3>

    <%= render "form", category: @category %>
  </div>
</div>
